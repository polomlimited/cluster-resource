apiVersion: gateway.nginx.org/v1alpha1
kind: ClientSettingsPolicy
metadata:
  name: large-uploads-policy
  namespace: default
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: polom-gateway  # Or attach to specific HTTPRoutes
  body:
    maxSize: "10m"
